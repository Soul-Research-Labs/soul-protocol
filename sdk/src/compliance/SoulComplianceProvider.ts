export class SoulComplianceProvider {
  constructor(public providerInfo: any) {}

  async registerProvider() {
    // Register as KYC provider
  }

  async issueCredential(user: string, credentialData: any) {
    // Issue credential
  }
}
