[package]
name = "shielded_pool"
type = "bin"
authors = ["ZASEON"]
compiler_version = ">=0.33.0"
description = "ZK circuit for shielded pool deposits and withdrawals with Merkle tree membership proofs"

[dependencies]
poseidon = { tag = "v0.2.3", git = "https://github.com/noir-lang/poseidon" }
