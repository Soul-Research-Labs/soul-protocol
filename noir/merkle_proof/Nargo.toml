[package]
name = "merkle_proof"
type = "bin"
authors = ["Soul Protocol"]
compiler_version = ">=1.0.0"
description = "Optimized Merkle tree inclusion proof for nullifier registry using Poseidon hashing"

[dependencies]
