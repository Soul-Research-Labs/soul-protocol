[package]
name = "sanctions_check"
type = "bin"
authors = ["Soul Protocol"]
description = "Privacy-preserving sanctions screening - proves an address is NOT on a sanctions list without revealing identity"

[dependencies]
poseidon = { tag = "v0.2.3", git = "https://github.com/noir-lang/poseidon" }
