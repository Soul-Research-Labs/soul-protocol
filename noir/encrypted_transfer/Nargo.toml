[package]
name = "encrypted_transfer"
type = "bin"
authors = ["ZASEON"]
compiler_version = ">=0.30.0"
description = "ZK proof for encrypted token transfers without revealing amounts"

[dependencies]
poseidon = { tag = "v0.2.6", git = "https://github.com/noir-lang/poseidon" }
